import { ForgotPasswordForm } from "../csr/ForgotPasswordForm";

/**
 * TODO(Phase 4 (Authentication)): redirect away if a session already exists.
 */
export const AuthForgotPassword = async () => {
  return <ForgotPasswordForm />;
};
