"use client";

import { FeaturePlaceholder } from "@/components/shared/feature-placeholder";

/**
 * Real React Hook Form + Zod form lands in Phase 4 (Authentication).
 */
export const ResetPasswordForm = () => {
  return (
    <FeaturePlaceholder
      title="Reset password"
      description="This form will be built in Phase 4 (Authentication)."
    />
  );
};
